import { keys, values } from 'lodash'

export const COLOR_MAP = {
	Todd: '#00B20E',
	Richard: '#0800FF',
	Jin: '#ff00aa',
}

export const USER_NAMES = keys(COLOR_MAP)
export const USER_COLORS = values(COLOR_MAP)
